require "spec_helper"

describe MoralesViewTool do
  it "has a version number" do
    expect(MoralesViewTool::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
